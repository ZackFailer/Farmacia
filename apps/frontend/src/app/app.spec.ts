import { TestBed } from '@angular/core/testing';

describe('AppComponent shell', () => {
  it('tests placeholder - AppComponent requires Ionic runtime', () => {
    expect(true).toBe(true);
  });
});
